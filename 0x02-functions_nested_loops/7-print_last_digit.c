#include "main.h"

/**
 * print_the_digit - Prints the last digits of a number
 * @n: The number to be treated
 * Return: Value of the last digit of number
 */

{
	int last;

	last = n % 10;
	if (last < 0)
   	{
	last = last = -1;
	_putchar(last + 0);

	return (last);
}
